/// {{Title}} stream sample model (scaffold).
library;

import 'package:meta/meta.dart';

/// One {{name}} stream tick returned by [{{Pascal}}Domain.{{streamMethod}}].
@immutable
class {{Pascal}}StreamSample {
  /// Creates one {{name}} stream sample.
  const {{Pascal}}StreamSample({
    required this.placeholder,
  });

  /// Placeholder metric until the TDD field table is authored.
  final double placeholder;
}
