/// CPU snapshot model (placeholder until M1-07).
library;

import 'package:meta/meta.dart';

/// CPU snapshot returned by CpuDomain.snapshot.
@immutable
class CpuInfo {
  const CpuInfo();
}

/// One CPU load stream sample (placeholder until M1-07).
@immutable
class CpuLoadSample {
  const CpuLoadSample();
}
