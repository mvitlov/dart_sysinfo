/// Runtime capability registry — not implemented in M1.
///
/// P1 capability and permission rows live in repo `docs/capability-matrix.md`
/// (M1-13). A queryable in-process registry is deferred to M3
/// domain-completeness work unless a new story is added — see EPICS.md
/// "Known M1 deferrals".
library;
